rep(6,5)  #把6重复5次
seq(1,20,4)  #从1到20，每次＋4
pmax(rep(6,5),seq(1,20,4))  #两个向量对应位置的最大值
which(c(1,5,2,4,6)>=3)  #向量中大于3的元素的下标
t(matrix(1:12,3))  #3*4的矩阵元素1~12，求它的转置
matrix(1:12,3)*matrix(1:12,3)  #两个矩阵的对应位置元素乘积
det(matrix(c((1:3)^0,(1:3)^1,(1:3)^2),3))  #矩阵的行列式
diff(diff(diff((1:5)^3)))  #返回向量中后一个元素减去前一个元素的差值，构成的新的向量
cov(c(1,2,3,3),c(-2,0,2,2))  #两个向量的协方差
integrate(sin,-pi,pi)  #sinx在-pi到pi上的积分